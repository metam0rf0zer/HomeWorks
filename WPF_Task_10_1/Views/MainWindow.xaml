<Window x:Class="WPF_Task_10_1.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WPF_Task_10_1.ViewModels"
        xmlns:models="clr-namespace:WPF_Task_10_1.Models"
        Title="Вход в систему" Height="250" Width="350"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <models:BoolToBackgroundConverter x:Key="BoolToBackground"/>


        <vm:LoginViewModel x:Key="LoginViewModel"/>
    </Window.Resources>

    <Grid DataContext="{StaticResource LoginViewModel}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Логин:" Grid.Row="0" Grid.Column="0" Margin="0,0,10,0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                 VerticalAlignment="Top"
                 Grid.Row="0" Grid.Column="1" Margin="10,1,0,0" Height="25"/>

        <TextBlock Text="Пароль:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="1" Grid.Column="1" Margin="10,4,0,6" Height="25"/>

        <Button Content="Войти"
                Command="{Binding LoginCommand}"
                Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                Margin="0,10,0,10" Height="30"/>

        <Border 
            Grid.Row="3" 
            Grid.Column="0" 
            Grid.ColumnSpan="2"
            BorderBrush="Gray"             
            Background="{Binding StatusMessage, Converter={StaticResource BoolToBackground}}"
            Padding="10"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            MinHeight="40" 
            MinWidth="200">

            <TextBlock 
            Text="{Binding StatusMessage}"
            TextWrapping="Wrap"
            FontSize="12" 
            Foreground="Black"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            TextAlignment="Center"/>

        </Border>
    </Grid>
</Window>